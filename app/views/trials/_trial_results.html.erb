<%= render "trial_count" %>

<% if show_many_trials?(@trial_filter_form.trials) %>
  <%= render "many_trials_notice" %>


<% end %>




<ul class="trial-list">
  <%= render trials.map { |trial| TrialPresenter.new(trial) } %>
</ul>

<%= render "pagination" %>
