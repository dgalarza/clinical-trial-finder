<div class="trial-results">
  <%= render "navigation" %>

  <h1><%= @trial.title %></h1>

  <div class="section">
    <h2><%= t(".study_purpose") %></h2>
    <p><%= @trial.description %></p>
  </div>

  <%= render "recruitment_table", trial: @trial %>

  <%= render "details_table", trial: @trial %>


  <div class="section">
    <h2><%= t(".contact_trial_team") %></h2>
    <p><%= t(".contact_instructions") %></p>

    <%= render @trial.ordered_sites(session[:zip_code_coordinates]) %>
  </div>
</div>

<%= render "trial_sidebar", trial: @trial %>
